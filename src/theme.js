import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#001739",
      light: "#c0d3ff",
    },
    text: {
      primary: "#001739",
    },
    background: {
      default: "#001739",
      paper: "#ffffff",
    },
  },
});
